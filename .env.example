# GeminiLoop Environment Variables

# Required: Google AI Studio API Key
# Get one here: https://makersuite.google.com/app/apikey
GOOGLE_AI_STUDIO_API_KEY=your_api_key_here

# Optional: RunPod Configuration
RUNS_DIR=/app/runs
PREVIEW_PORT=8080
HEADLESS=true

# Optional: OpenHands (future integration)
# OPENHANDS_API_KEY=your_openhands_key

# OpenHands Configuration
# Mode: mock (regex-based demo) or local (subprocess CLI)
OPENHANDS_MODE=mock

# Optional: OpenHands API endpoint (future)
# OPENHANDS_API_URL=http://localhost:3000

# Visible Browser Mode (for demos)
# Set to 1 to enable noVNC browser visibility
VISIBLE_BROWSER=0

# When visible browser is enabled:
# - Browser runs in Xvfb (virtual X server)
# - Accessible via noVNC at http://localhost:6080/vnc.html
# - Password: secret

# GitHub Integration (optional)
# Set to enable template branching and auto-commit
GITHUB_TOKEN=
GITHUB_REPO=owner/repo
BASE_BRANCH=main
